#!/bin/bash -l

# module load berkeleygw-tutorial

# This parallel script launches BGW to perform a self-energy calculation.

# srun -n 4 -c 32 --cpu-bind=cores sigma.real.x &> sigma.out

$CMD_PREFIX_BGW sigma.real.x | tee sigma.out
